@using Shared.RestClient.Interfaces
@inject IApplicationUnit _unit;

<div class="categories">
    <h2>دسته‌بندی‌ها</h2>
    <ul>
        @foreach (var item in _categories)
        {
            <li @onclick="()=> ShowItems(item)">‌ @item.Title</li>
        }
    </ul>
</div>